# User fixture
<% SALT = "NaCl" unless defined?(SALT) %>

admin:
  username: admin
  name: admin
  hashed_password: <%= User.encrypted_password('admin', SALT) %>
  salt: <%= SALT %>

joao:
  username: joao
  name: joao
  hashed_password: <%= User.encrypted_password('joao', SALT) %>
  salt: <%= SALT %>

pacheco:
  username: pacheco
  name: pacheco
  hashed_password: <%= User.encrypted_password('pacheco', SALT) %>
  salt: <%= SALT %>

telmo:
  username: telmo
  name: telmo
  hashed_password: <%= User.encrypted_password('telmo', SALT) %>
  salt: <%= SALT %>

simao:
  username: simao
  name: simao
  hashed_password: <%= User.encrypted_password('simao', SALT) %>
  salt: <%= SALT %>
